# i18n 多语言重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 Next.js 14 App Router 项目中，基于已安装的 next-intl v3.26.3，将路由全量迁移到 `app/[locale]/` 下，支持 EN（默认）+ ZH-Hans + JA + DE 四种语言。

**Architecture:** 将 `app/(main)/` 和 `app/(auth)/` 全部移入 `app/[locale]/`，next-intl middleware 负责 locale 检测和重定向，middleware 通过 `x-next-intl-locale` header 传递 locale 给根 layout，受保护路由通过 `getToken` 手动鉴权（移除 `withAuth` 包裹器）。翻译文件按 namespace 拆分存放于 `messages/{locale}/{namespace}.json`。

**Tech Stack:** next-intl v3.26.3, NextAuth v4, Next.js 14 App Router, TypeScript, shadcn/ui DropdownMenu

---

## 文件变更总览

### 阶段一：基础设施（不动页面，不破坏现有功能）

| 操作 | 文件 |
|------|------|
| Modify | `i18n/locale.ts` — 添加 zh/ja/de |
| Modify | `i18n/routing.ts` — 清理幽灵路径，pathnames 改为实际路由 |
| Modify | `middleware.ts` — 替换 withAuth 为 next-intl + getToken |
| Modify | `i18n/request.ts` — 改为多 namespace 合并加载（**与 messages/ 重构同步提交**） |
| Create | `messages/en/common.json` — Navbar、Footer、通用文案 |
| Create | `messages/en/home.json` |
| Create | `messages/en/pricing.json` |
| Create | `messages/en/about.json` |
| Create | `messages/en/contact.json` |
| Create | `messages/en/tools.json` — 工具落地页 |
| Create | `messages/en/video-tools.json` — 工具页 |
| Delete | `messages/en.json` — 单文件废弃（迁移内容到上面） |
| Create | `app/[locale]/layout.tsx` — generateStaticParams + NextIntlClientProvider |
| Modify | `app/layout.tsx` — lang 改为从 header 读取 |
| Create | `lib/seo/alternate-links.ts` — hreflang 工具函数 |

### 阶段二：路由迁移（大批量文件移动）

| 操作 | 文件 |
|------|------|
| Move | `app/(main)/*` → `app/[locale]/(main)/*` （所有页面） |
| Move | `app/(auth)/*` → `app/[locale]/(auth)/*` |
| Modify | `app/[locale]/(main)/layout.tsx` — 原 (main)/layout.tsx，添加 locale 感知 |
| Modify | `app/[locale]/(auth)/layout.tsx` — 原 (auth)/layout.tsx |
| Modify | 所有移动后的 `page.tsx` — 加 `setRequestLocale(locale)` |

### 阶段三：翻译功能

| 操作 | 文件 |
|------|------|
| Modify | `components/navbar.tsx` — 添加语言切换器组件 |
| Create | `components/common/locale-switcher.tsx` — 语言切换下拉组件 |
| Create | `messages/zh/common.json` + 各页面 namespace |
| Create | `messages/ja/common.json` + 各页面 namespace |
| Create | `messages/de/common.json` + 各页面 namespace |
| Modify | 各需翻译的 `page.tsx` — 接入 getTranslations + generateMetadata |
| Delete | `lib/i18n.ts` — 废弃，替换所有调用 |
| Delete | `locales/` 目录 — 迁移完成后删除 |

---

## Task 1: 激活 i18n locale 配置

**Files:**
- Modify: `i18n/locale.ts`
- Modify: `i18n/routing.ts`

- [ ] **Step 1: 更新 `i18n/locale.ts`，添加 zh/ja/de**

```ts
// i18n/locale.ts
export const locales = ['en', 'zh', 'ja', 'de'] as const;
export type Locale = (typeof locales)[number];

export const defaultLocale: Locale = 'en';

export const localeMetadata: Record<Locale, {
  name: string;
  nativeName: string;
  direction: 'ltr' | 'rtl';
  region: string;
}> = {
  en: { name: 'English', nativeName: 'English', direction: 'ltr', region: 'US' },
  zh: { name: 'Chinese (Simplified)', nativeName: '简体中文', direction: 'ltr', region: 'CN' },
  ja: { name: 'Japanese', nativeName: '日本語', direction: 'ltr', region: 'JP' },
  de: { name: 'German', nativeName: 'Deutsch', direction: 'ltr', region: 'DE' },
};

export function isValidLocale(locale: string): locale is Locale {
  return locales.includes(locale as Locale);
}

export function getLocaleMetadata(locale: Locale) {
  return localeMetadata[locale];
}

export function getPreferredLocale(acceptLanguage?: string): Locale {
  if (!acceptLanguage) return defaultLocale;
  const languages = acceptLanguage
    .split(',')
    .map(lang => {
      const [code, q] = lang.trim().split(';q=');
      return { code: code.toLowerCase().split('-')[0], quality: q ? parseFloat(q) : 1 };
    })
    .sort((a, b) => b.quality - a.quality);
  for (const { code } of languages) {
    if (isValidLocale(code)) return code;
  }
  return defaultLocale;
}
```

- [ ] **Step 2: 更新 `i18n/routing.ts`，清理幽灵路径**

```ts
// i18n/routing.ts
import { defineRouting } from 'next-intl/routing';
import { createNavigation } from 'next-intl/navigation';
import { locales, defaultLocale } from './locale';

export const routing = defineRouting({
  locales,
  defaultLocale,
  localePrefix: 'as-needed',
  // 不定义 pathnames（使用默认行为，路径名与 URL 一致）
});

export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

- [ ] **Step 3: 验证 TypeScript 编译无报错**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add i18n/locale.ts i18n/routing.ts
git commit -m "feat(i18n): activate zh/ja/de locales in routing config"
```

---

## Task 2: 重写 middleware.ts

**Files:**
- Modify: `middleware.ts`

> **重要：** 这是破坏性改动。新 middleware 移除 `withAuth` 包裹器，改用 `getToken` 手动鉴权。所有原有逻辑（studio rewrite、auth redirect、protected routes）必须完整保留。

- [ ] **Step 1: 完整替换 `middleware.ts`**

```ts
// middleware.ts
import createMiddleware from 'next-intl/middleware';
import { getToken } from 'next-auth/jwt';
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { routing } from './i18n/routing';

const intlMiddleware = createMiddleware(routing);

// Auth routes - redirect to studio if already logged in
const AUTH_ROUTES = ['/login', '/signup'];

// Protected routes (path prefix matching, after stripping locale prefix)
const PROTECTED_ROUTES = ['/profile', '/settings', '/video', '/subscription'];

function getStrippedPath(pathname: string): string {
  // Strip non-default locale prefixes: /zh/foo → /foo, /ja/foo → /foo
  const nonDefaultLocales = routing.locales.filter(l => l !== routing.defaultLocale);
  const pattern = new RegExp(`^\\/(${nonDefaultLocales.join('|')})(\/|$)`);
  return pathname.replace(pattern, '/');
}

function isAuthPath(stripped: string): boolean {
  return AUTH_ROUTES.some(r => stripped === r || stripped.startsWith(r + '/'));
}

function isProtectedPath(stripped: string): boolean {
  return PROTECTED_ROUTES.some(r => stripped.startsWith(r));
}

function isStaticAsset(pathname: string): boolean {
  return (
    pathname.startsWith('/_next') ||
    pathname.startsWith('/_vercel') ||
    pathname.includes('.')
  );
}

export default async function middleware(req: NextRequest) {
  const { nextUrl } = req;
  const pathname = nextUrl.pathname;

  // 1. Skip for static assets
  if (isStaticAsset(pathname)) {
    return NextResponse.next();
  }

  // 2. Skip for API routes
  if (pathname.startsWith('/api/')) {
    return NextResponse.next();
  }

  // 3. Admin routes: skip locale detection (always English, no redirect)
  if (pathname.includes('/admin')) {
    return NextResponse.next();
  }

  // 4. Studio rewrite (runs before intl, preserves browser URL)
  if (pathname.startsWith('/studio/')) {
    const specialPaths = ['/studio/video-agent-beta', '/studio/plans'];
    if (specialPaths.some(p => pathname.startsWith(p))) {
      return NextResponse.next();
    }
    const toolMap: Record<string, string> = {
      discover: 'discover',
      'text-to-video': 'text-to-video',
      'image-to-video': 'image-to-video',
      'ai-video-effects': 'video-effects',
      'text-to-image': 'text-to-image',
      'image-to-image': 'image-to-image',
      'my-assets': 'my-assets',
      plans: 'my-profile',
    };
    const pathParts = pathname.split('/').filter(Boolean);
    const toolPath = pathParts[1];
    const tool = toolMap[toolPath] || 'discover';
    const rewriteUrl = new URL('/create', nextUrl.origin);
    rewriteUrl.searchParams.set('tool', tool);
    nextUrl.searchParams.forEach((value, key) => rewriteUrl.searchParams.set(key, value));
    return NextResponse.rewrite(rewriteUrl);
  }

  // 5. Run next-intl middleware (locale detection + redirect)
  const intlResponse = intlMiddleware(req);

  // Determine detected locale from intl response cookie or request
  const detectedLocale =
    intlResponse?.cookies.get('NEXT_LOCALE')?.value ||
    req.cookies.get('NEXT_LOCALE')?.value ||
    routing.defaultLocale;

  // Build response to mutate (clone intlResponse or create fresh)
  const response = intlResponse || NextResponse.next();

  // Inject locale into header for root layout to read
  response.headers.set('x-next-intl-locale', detectedLocale);

  // 6. Auth and protected route logic
  const stripped = getStrippedPath(pathname);
  const token = await getToken({ req, secret: process.env.NEXTAUTH_SECRET });

  // Auth pages: redirect to studio if already logged in
  if (isAuthPath(stripped)) {
    if (token) {
      return NextResponse.redirect(new URL('/studio/discover', nextUrl.origin));
    }
    return response;
  }

  // Protected routes: redirect to login if not authenticated
  if (isProtectedPath(stripped)) {
    if (!token) {
      const locale = detectedLocale === routing.defaultLocale ? '' : `/${detectedLocale}`;
      const loginUrl = new URL(`${locale}/login`, nextUrl.origin);
      loginUrl.searchParams.set('callbackUrl', encodeURIComponent(pathname + (nextUrl.search || '')));
      return NextResponse.redirect(loginUrl);
    }
    return response;
  }

  return response;
}

export const config = {
  // 注意：不排除 studio，因为 studio rewrite 在 middleware 内处理
  // 但 studio 在 middleware 内部提前 return，不会触发 intl locale 检测
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)', '/'],
};
```

- [ ] **Step 2: TypeScript 编译检查**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

Expected: 无错误（或只有与 i18n 无关的现有错误）

- [ ] **Step 3: 本地启动验证基本功能不破坏**

```bash
bash scripts/dev.sh
```

手动检查：
- `http://localhost:3000/` → 首页正常加载
- `http://localhost:3000/pricing` → 正常加载
- `http://localhost:3000/login` → 正常加载
- `http://localhost:3000/profile` → 未登录时跳转 `/login`
- `http://localhost:3000/studio/text-to-video` → 正常 rewrite 到 /create

- [ ] **Step 4: Commit**

```bash
git add middleware.ts
git commit -m "feat(i18n): replace withAuth with next-intl middleware + getToken auth"
```

---

## Task 3: 重构翻译文件结构（与 request.ts 同步提交）

**Files:**
- Create: `messages/en/common.json`
- Create: `messages/en/home.json`
- Create: `messages/en/pricing.json`
- Create: `messages/en/about.json`
- Create: `messages/en/contact.json`
- Create: `messages/en/tools.json`
- Create: `messages/en/video-tools.json`
- Delete: `messages/en.json`
- Modify: `i18n/request.ts`

> **原子操作警告：** `messages/en/` 目录创建和 `request.ts` 修改必须在同一次 commit 中完成。如果只做其中一个，应用会崩溃（找不到翻译文件）。

- [ ] **Step 1: 创建 `messages/en/` 目录和文件**

先查看现有 `messages/en.json` 内容，将其拆分到各 namespace：

```bash
cat /Users/jacob/Desktop/vidfab/messages/en.json
```

然后创建目录和文件（内容来自原 `messages/en.json` 对应字段，以及 `locales/en.json`）：

```bash
mkdir -p /Users/jacob/Desktop/vidfab/messages/en
```

- [ ] **Step 2: 写 `messages/en/common.json`**

包含 Navbar、Footer、通用按钮、错误文案。最小化示例（根据实际 `messages/en.json` 内容补充）：

```json
{
  "nav": {
    "tools": "Tools",
    "pricing": "Pricing",
    "blog": "Blog",
    "login": "Sign In",
    "signup": "Get Started"
  },
  "footer": {
    "copyright": "© 2025 VidFab. All rights reserved.",
    "privacy": "Privacy Policy",
    "terms": "Terms of Service"
  },
  "common": {
    "loading": "Loading...",
    "error": "Something went wrong",
    "retry": "Try Again"
  }
}
```

- [ ] **Step 3: 写 `messages/en/home.json`**

包含首页 hero、features 等文案（从 `locales/en.json` 的 `homepage` 字段迁移）：

```json
{
  "meta": {
    "title": "VidFab - AI Video Platform | Transform Your Videos with AI",
    "description": "Transform your videos with cutting-edge AI technology."
  },
  "hero": {
    "title": "Create Stunning AI Videos",
    "subtitle": "Transform text and images into professional videos in seconds"
  }
}
```

- [ ] **Step 4: 写其余 namespace 文件**

`messages/en/pricing.json`，`messages/en/about.json`，`messages/en/contact.json`，`messages/en/tools.json`，`messages/en/video-tools.json`。

各文件暂时放 `meta.title` 和 `meta.description` 作为 MVP，后续阶段三再补充完整文案：

```json
{
  "meta": {
    "title": "Pricing - VidFab",
    "description": "Simple, transparent pricing for everyone."
  }
}
```

- [ ] **Step 5: 同步修改 `i18n/request.ts`**

```ts
// i18n/request.ts
import { getRequestConfig } from 'next-intl/server';
import { defaultLocale, isValidLocale, type Locale } from './locale';
import { routing } from './routing';

const NAMESPACES = ['common', 'home', 'pricing', 'about', 'contact', 'tools', 'video-tools'];

async function loadMessages(locale: Locale) {
  const entries = await Promise.all(
    NAMESPACES.map(async (ns) => {
      try {
        const mod = await import(`../messages/${locale}/${ns}.json`);
        return [ns, mod.default] as const;
      } catch {
        // Fallback to English if translation missing
        const mod = await import(`../messages/en/${ns}.json`);
        return [ns, mod.default] as const;
      }
    })
  );
  return Object.fromEntries(entries);
}

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !isValidLocale(locale)) {
    locale = defaultLocale;
  }

  return {
    locale,
    messages: await loadMessages(locale as Locale),
    timeZone: 'UTC',
    formats: {
      dateTime: {
        short: { day: 'numeric', month: 'short', year: 'numeric' },
      },
      number: {
        currency: { style: 'currency', currency: 'USD' },
      },
    },
  };
});
```

- [ ] **Step 6: TypeScript 编译检查**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

> **预期：** 此步骤可能报 `Cannot find module '../messages/en/common.json'` 等错误，这是正常的——因为 `messages/en.json`（单文件）还未被删除，但 `request.ts` 已改为多文件加载。Step 7 的原子提交会让两者同时生效，服务启动后才能真正验证。

- [ ] **Step 7: 原子提交（messages/ + request.ts 同步）**

```bash
git add messages/en/ i18n/request.ts
git rm messages/en.json
git commit -m "feat(i18n): migrate to namespace-based messages structure"
```

---

## Task 4: 创建 `app/[locale]/layout.tsx`

**Files:**
- Create: `app/[locale]/layout.tsx`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p /Users/jacob/Desktop/vidfab/app/\[locale\]
```

- [ ] **Step 2: 写 `app/[locale]/layout.tsx`**

```tsx
// app/[locale]/layout.tsx
import { NextIntlClientProvider } from 'next-intl';
import { getMessages, setRequestLocale } from 'next-intl/server';
import { routing } from '@/i18n/routing';
import { notFound } from 'next/navigation';
import type { Locale } from '@/i18n/locale';

interface Props {
  children: React.ReactNode;
  params: { locale: string };
}

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = params;

  // Validate locale
  if (!routing.locales.includes(locale as Locale)) {
    notFound();
  }

  // Enable static rendering for this locale
  setRequestLocale(locale);

  // Load messages for this locale (populated by request.ts)
  const messages = await getMessages();

  return (
    <NextIntlClientProvider messages={messages}>
      {children}
    </NextIntlClientProvider>
  );
}
```

- [ ] **Step 3: 验证编译**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add app/\[locale\]/layout.tsx
git commit -m "feat(i18n): add [locale] layout with NextIntlClientProvider and generateStaticParams"
```

---

## Task 5: 更新根 `app/layout.tsx` 读取 locale header

**Files:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: 在根 layout 顶部添加 locale header 读取**

对 `app/layout.tsx` 做两处精确修改（不改其他任何内容）：

**修改 1 — 添加 import（在现有最后一个 import 行后追加）：**

```tsx
import { headers } from 'next/headers';
```

**修改 2 — 将 RootLayout 函数签名和 html 标签改为动态读取 locale：**

原来：
```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
```

改为：
```tsx
export default async function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const headersList = headers();
  const locale = headersList.get('x-next-intl-locale') ?? 'en';
  const lang = locale === 'zh' ? 'zh-Hans' : locale;

  return (
    <html lang={lang} suppressHydrationWarning>
```

> 注意：只改这两处，`<body>` 及其内部所有 Providers、GTM 脚本、字体等完全保持不变。

- [ ] **Step 2: 验证编译**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 3: 本地验证 lang 属性**

```bash
bash scripts/dev.sh
```

访问 `http://localhost:3000/`，检查 `<html lang>` 属性值为 `en`。

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx
git commit -m "feat(i18n): read html lang from x-next-intl-locale header"
```

---

## Task 6: 迁移 `(main)` 路由到 `[locale]/(main)`

**Files:**
- Move: `app/(main)/` → `app/[locale]/(main)/`（所有文件）
- Modify: 移动后各 `page.tsx`（Server Component）加 `setRequestLocale`

> **注意：** 这是本次重构最大的单步操作。移动后立即验证应用是否正常工作。

- [ ] **Step 1: 移动整个 (main) 目录**

```bash
mv /Users/jacob/Desktop/vidfab/app/\(main\) /Users/jacob/Desktop/vidfab/app/\[locale\]/\(main\)
```

- [ ] **Step 2: 更新 `app/[locale]/(main)/layout.tsx`**

原 `(main)/layout.tsx` 是 Client Component（`"use client"`，有 scroll 状态）。这里只需确认它不再导入任何路径已变化的模块：

```tsx
// app/[locale]/(main)/layout.tsx — 保持原有 "use client" 不变
// 无需修改，layout 不需要访问 locale
```

- [ ] **Step 3: 在各页面 Server Component 顶部加 `setRequestLocale`**

对以下文件（有 `async` 函数的页面，即 Server Components）添加：

```tsx
import { setRequestLocale } from 'next-intl/server';

export default async function PageName({ params }: { params: { locale: string } }) {
  setRequestLocale(params.locale);
  // 原有内容不变
```

需要修改的页面（仅 Server Component，即没有 `"use client"` 的 page.tsx）：
- `app/[locale]/(main)/page.tsx`（首页）
- `app/[locale]/(main)/about/page.tsx`
- `app/[locale]/(main)/pricing/page.tsx`
- `app/[locale]/(main)/contact/page.tsx`
- `app/[locale]/(main)/text-to-video/page.tsx`（若是 Server Component）
- `app/[locale]/(main)/image-to-video/page.tsx`
- `app/[locale]/(main)/ai-video-effects/page.tsx`
- `app/[locale]/(main)/text-to-image/page.tsx`
- `app/[locale]/(main)/image-to-image/page.tsx`
- `app/[locale]/(main)/tools/veo3/page.tsx`
- `app/[locale]/(main)/tools/sora2/page.tsx`
- `app/[locale]/(main)/tools/kling3/page.tsx`

- [ ] **Step 4: 验证编译**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -50
```

- [ ] **Step 5: 本地启动验证**

```bash
bash scripts/dev.sh
```

逐一访问：
- `http://localhost:3000/` → 首页正常
- `http://localhost:3000/pricing` → 正常
- `http://localhost:3000/about` → 正常
- `http://localhost:3000/tools/veo3` → 正常

- [ ] **Step 6: Commit**

```bash
git add app/\[locale\]/\(main\)/
git commit -m "feat(i18n): move (main) pages to [locale]/(main)"
```

---

## Task 7: 迁移 `(auth)` 路由到 `[locale]/(auth)`

**Files:**
- Move: `app/(auth)/` → `app/[locale]/(auth)/`

- [ ] **Step 1: 移动 (auth) 目录**

```bash
mv /Users/jacob/Desktop/vidfab/app/\(auth\) /Users/jacob/Desktop/vidfab/app/\[locale\]/\(auth\)
```

- [ ] **Step 2: 验证编译**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 3: 验证登录流程**

```bash
bash scripts/dev.sh
```

- `http://localhost:3000/login` → 正常加载（英文无前缀）
- 模拟中文访问 `http://localhost:3000/zh/login` → 正常加载

- [ ] **Step 4: Commit**

```bash
git add app/\[locale\]/\(auth\)/
git commit -m "feat(i18n): move (auth) pages to [locale]/(auth)"
```

---

## Task 8: 替换内部 `<Link>` 为 next-intl Link

**Files:**
- Modify: 所有使用 `next/link` 的组件文件（在 `components/` 和 `app/[locale]/` 下）

> next-intl 的 `Link` 自动携带当前 locale 前缀，无需手动处理。

- [ ] **Step 1: 全局搜索 `from 'next/link'` 的用法**

```bash
grep -r "from 'next/link'" /Users/jacob/Desktop/vidfab/components/ \
  /Users/jacob/Desktop/vidfab/app/\[locale\]/ \
  --include="*.tsx" --include="*.ts" -l
```

- [ ] **Step 2: 批量替换 import（组件层）**

对每个找到的文件，将：
```tsx
import Link from 'next/link';
```
替换为：
```tsx
import { Link } from '@/i18n/routing';
```

> **例外：** API 路由、服务端工具函数不需要替换（它们不渲染 Link）。
> **例外：** admin 相关组件可以保留 next/link（admin 无 locale 前缀需求）。

- [ ] **Step 3: 验证编译**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: 验证页面间导航**

```bash
bash scripts/dev.sh
```

点击 Navbar 中的链接，确认路径正确（英文无前缀，后续切换中文后自动加 `/zh/`）。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(i18n): replace next/link with next-intl Link for locale-aware navigation"
```

---

## Task 9: 新增 `lib/seo/alternate-links.ts`

**Files:**
- Create: `lib/seo/alternate-links.ts`

- [ ] **Step 1: 创建目录并写工具函数**

```bash
mkdir -p /Users/jacob/Desktop/vidfab/lib/seo
```

```ts
// lib/seo/alternate-links.ts
import { routing } from '@/i18n/routing';

const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL || 'https://vidfab.ai';

/**
 * 生成 hreflang alternate 链接映射
 * @param path 路径（不含 locale 前缀），如 '/pricing'
 * @returns hreflang map，包含所有 locale 及 x-default
 *
 * 示例：getAlternateLinks('/pricing')
 * → {
 *     en: 'https://vidfab.ai/pricing',
 *     zh: 'https://vidfab.ai/zh/pricing',
 *     ja: 'https://vidfab.ai/ja/pricing',
 *     de: 'https://vidfab.ai/de/pricing',
 *     'x-default': 'https://vidfab.ai/pricing',
 *   }
 */
export function getAlternateLinks(path: string): Record<string, string> {
  const result: Record<string, string> = {};
  for (const locale of routing.locales) {
    const prefix = locale === routing.defaultLocale ? '' : `/${locale}`;
    result[locale] = `${BASE_URL}${prefix}${path}`;
  }
  // x-default points to the default locale (English)
  result['x-default'] = `${BASE_URL}${path}`;
  return result;
}
```

- [ ] **Step 2: 编译验证**

```bash
cd /Users/jacob/Desktop/vidfab && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 3: Commit**

```bash
git add lib/seo/alternate-links.ts
git commit -m "feat(i18n): add getAlternateLinks util for hreflang SEO"
```

---

## Task 10: 添加语言切换器组件

**Files:**
- Create: `components/common/locale-switcher.tsx`
- Modify: `components/navbar.tsx`

- [ ] **Step 1: 创建 `components/common/locale-switcher.tsx`**

```tsx
// components/common/locale-switcher.tsx
'use client';

import { useTransition } from 'react';
import { Globe } from 'lucide-react';
import { useRouter, usePathname } from '@/i18n/routing';
import { useLocale } from 'next-intl';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';
import { Button } from '@/components/ui/button';

const LOCALES = [
  { code: 'en', label: 'English',  short: 'EN' },
  { code: 'zh', label: '中文',     short: '中文' },
  { code: 'ja', label: '日本語',   short: '日本語' },
  { code: 'de', label: 'Deutsch',  short: 'DE' },
] as const;

export function LocaleSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();
  const [isPending, startTransition] = useTransition();

  function handleLocaleChange(nextLocale: string) {
    startTransition(() => {
      router.replace(pathname, { locale: nextLocale });
    });
  }

  const current = LOCALES.find(l => l.code === locale);

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button
          variant="ghost"
          size="sm"
          className="gap-1.5 text-sm"
          disabled={isPending}
          aria-label="Select language"
        >
          <Globe className="h-4 w-4" />
          <span>{current?.short ?? 'EN'}</span>
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        {LOCALES.map(({ code, label }) => (
          <DropdownMenuItem
            key={code}
            onClick={() => handleLocaleChange(code)}
            className={locale === code ? 'font-semibold' : ''}
          >
            {label}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 2: 在 `components/navbar.tsx` 中引入并放置**

找到 Navbar 中桌面版右侧操作区（通常是 login/signup 按钮附近），插入：

```tsx
import { LocaleSwitcher } from '@/components/common/locale-switcher';

// 在桌面版导航右侧区域插入：
<LocaleSwitcher />
```

- [ ] **Step 3: 本地验证语言切换**

```bash
bash scripts/dev.sh
```

- 点击语言切换器选择「中文」→ URL 变为 `/zh/`，页面刷新
- 再选「English」→ URL 变为 `/`（无前缀）

- [ ] **Step 4: Commit**

```bash
git add components/common/locale-switcher.tsx components/navbar.tsx
git commit -m "feat(i18n): add LocaleSwitcher component to Navbar"
```

---

## Task 11: 为首页和核心页面接入翻译

**Files:**
- Modify: `app/[locale]/(main)/page.tsx`（首页）
- Modify: `app/[locale]/(main)/pricing/page.tsx`

> 这是翻译接入的模板任务。其余页面按此模式复制。

- [ ] **Step 1: 更新首页 `generateMetadata` + 正文使用 `getTranslations`**

```tsx
// app/[locale]/(main)/page.tsx
import { setRequestLocale, getTranslations } from 'next-intl/server';
import { getAlternateLinks } from '@/lib/seo/alternate-links';

export async function generateMetadata({ params }: { params: { locale: string } }) {
  setRequestLocale(params.locale);
  const t = await getTranslations({ locale: params.locale, namespace: 'home' });
  return {
    title: t('meta.title'),
    description: t('meta.description'),
    alternates: {
      canonical: `${process.env.NEXT_PUBLIC_BASE_URL || 'https://vidfab.ai'}${params.locale === 'en' ? '' : '/' + params.locale}`,
      languages: getAlternateLinks('/'),
    },
  };
}

export default async function HomePage({ params }: { params: { locale: string } }) {
  setRequestLocale(params.locale);
  // 原有 JSX 返回值保持不变，仅在文件顶部追加上述 generateMetadata 和 import
  // 不改动现有组件树，后续阶段再逐步替换硬编码文案
}
```

- [ ] **Step 2: 同步更新 zh/ja/de 翻译 stub**

```bash
mkdir -p /Users/jacob/Desktop/vidfab/messages/zh
mkdir -p /Users/jacob/Desktop/vidfab/messages/ja
mkdir -p /Users/jacob/Desktop/vidfab/messages/de
```

为每个 locale 创建 `home.json`（占位内容，后续人工翻译）：

```json
{
  "meta": {
    "title": "VidFab - AI 视频平台",
    "description": "使用 AI 技术轻松创建、增强和转换视频。"
  },
  "hero": {
    "title": "创建精彩 AI 视频",
    "subtitle": "秒级将文字和图片转换为专业视频"
  }
}
```

类似地为 ja 和 de 创建对应内容。

- [ ] **Step 3: 验证多语言路由**

```bash
bash scripts/dev.sh
```

- `http://localhost:3000/` → 英文 meta title 正确
- `http://localhost:3000/zh/` → 中文 meta title 正确
- `http://localhost:3000/ja/` → 日文 meta title 正确
- `http://localhost:3000/de/` → 德文 meta title 正确

检查 page source 中 `<html lang>` 和 `<hreflang>` 标签是否正确。

- [ ] **Step 4: Commit**

```bash
git add app/\[locale\]/\(main\)/page.tsx messages/
git commit -m "feat(i18n): wire home page translations + add zh/ja/de stubs"
```

---

## Task 12: 清理废弃文件

**Files:**
- Delete: `lib/i18n.ts`
- 注意：`locales/` 目录在本项目中不存在（翻译文件已在 `messages/` 下），无需删除

已知使用 `lib/i18n.ts` 的 6 个文件（均为 Client Component，使用 `useTranslation`）：
- `components/create/text-to-video-client.tsx` → namespace: `video-tools`
- `components/create/ai-video-effects-client.tsx` → namespace: `video-tools`
- `components/create/image-to-video-client.tsx` → namespace: `video-tools`
- `components/create/image-to-image-client.tsx` → namespace: `video-tools`
- `components/create/text-to-image-client.tsx` → namespace: `video-tools`
- `components/gradient-hero/hero-content.tsx` → namespace: `home`

- [ ] **Step 1: 确认当前使用情况（防止有新增调用）**

```bash
grep -r "from '@/lib/i18n'" /Users/jacob/Desktop/vidfab/app/ \
  /Users/jacob/Desktop/vidfab/components/ \
  --include="*.tsx" --include="*.ts" -l
```

- [ ] **Step 2: 替换 `components/create/*-client.tsx` 中的调用**

对上述 5 个 create 组件，每个文件做相同替换：

**原来：**
```tsx
import { useTranslation } from '@/lib/i18n';
const { t } = useTranslation();
// 用法如: t('textToVideo.prompt')
```

**替换为：**
```tsx
import { useTranslations } from 'next-intl';
const t = useTranslations('video-tools');
// 用法如: t('textToVideo.prompt')  ← key 路径保持不变
```

> 确保 `messages/en/video-tools.json` 中有对应的 key（Task 3 时已创建 stub，此时补充实际内容）。

- [ ] **Step 3: 替换 `components/gradient-hero/hero-content.tsx` 中的调用**

```tsx
import { useTranslations } from 'next-intl';
const t = useTranslations('home');
```

- [ ] **Step 4: 确认无残余引用**

```bash
grep -r "lib/i18n" /Users/jacob/Desktop/vidfab/app/ /Users/jacob/Desktop/vidfab/components/ --include="*.tsx" --include="*.ts"
# 应该无输出
```

- [ ] **Step 5: 删除 `lib/i18n.ts`**

```bash
git rm /Users/jacob/Desktop/vidfab/lib/i18n.ts
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore(i18n): replace lib/i18n.ts usage with next-intl useTranslations"
```

---

## 验收清单

完成上述所有任务后，逐项验证：

- [ ] `http://localhost:3000/` → `<html lang="en">` ✓
- [ ] `http://localhost:3000/zh/` → `<html lang="zh-Hans">` ✓
- [ ] `http://localhost:3000/ja/` → `<html lang="ja">` ✓
- [ ] `http://localhost:3000/de/` → `<html lang="de">` ✓
- [ ] `http://localhost:3000/en/` → 301 重定向到 `/` ✓（next-intl as-needed 自动处理）
- [ ] Navbar 语言切换器可见，切换后 URL 和 lang 属性正确更新 ✓
- [ ] 未登录访问 `/zh/subscription` → 重定向到 `/zh/login` ✓
- [ ] 已登录访问 `/zh/login` → 重定向到 `/studio/discover` ✓
- [ ] `/studio/text-to-video` → rewrite 正常工作，不被 locale 拦截 ✓
- [ ] `/admin/` → 无 locale 前缀，直接访问 ✓
- [ ] 首页 `<link rel="alternate" hreflang="zh" href="https://vidfab.ai/zh/">` 存在 ✓
- [ ] `npx tsc --noEmit` 无新增错误 ✓
