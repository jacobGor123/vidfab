# VidFab 云原生架构分阶段执行计划

## 📅 总体时间表

```
阶段 1: 准备阶段    (Day 1-7)    - 环境配置 + 账户注册
阶段 2: 前端迁移    (Day 8-14)   - Vercel 部署 + Redis 迁移
阶段 3: 后台迁移    (Day 15-21)  - Inngest + 视频处理
阶段 4: 测试验证    (Day 22-28)  - 完整测试 + 性能优化
阶段 5: 正式上线    (Day 29-35)  - 灰度发布 + 全量上线
────────────────────────────────────────────────────────
总计: 5周 (35天)
```

---

## 🎯 阶段 1: 准备阶段 (Day 1-7)

### 目标
- ✅ 注册所有云服务账户
- ✅ 配置开发环境
- ✅ 团队培训

### 详细任务

#### Day 1-2: 账户注册和配置

**任务清单**:

```markdown
[ ] 1. 注册 Vercel Pro 账户
    - 访问 https://vercel.com/signup
    - 绑定 GitHub/GitLab 仓库
    - 升级到 Pro 套餐 ($20/月)
    - 添加团队成员

[ ] 2. 注册 Inngest 账户
    - 访问 https://www.inngest.com/
    - 使用 GitHub 登录
    - 创建新项目 "vidfab-production"
    - 复制 Event Key 和 Signing Key

    Event Key:_4ZO2TrGPnhAwuB2FzWLIy08gGRExP1SeiuFXGGvg1bW3EMrJcSfoRl6jV69-GAD6GfZOZXDdk4FPnjWOwPMAw

    Signing Key: signkey-prod-516e24a2371cd0c54373567880a057d9a2de4186b53b80e8eab4186c6e647336

[ ] 3. 注册 Upstash Redis
    - 访问 https://upstash.com/
    - 创建新的 Redis 数据库
    - 选择 Free 套餐
    - 选择最近的区域 (Asia/Pacific)
    - 复制连接信息

[ ] 4. 注册 Cloudinary
    - 访问 https://cloudinary.com/users/register/free
    - 创建账户
    - 记录 Cloud Name, API Key, API Secret
    

[ ] 5. 注册 Axiom
    - 访问 https://axiom.co/
    - 创建账户
    - 创建新的 Dataset "vidfab-logs"
    - 获取 API Token
     API Tokn：xaat-09ae4741-7b18-4d3d-953f-96f268baa921
```

**输出物**:
- ✅ 所有账户注册完成
- ✅ 环境变量清单 (`env-variables.txt`)

---

#### Day 3-4: 本地开发环境配置

**任务清单**:

```markdown
[ ] 1. 创建 .env.vercel 文件
    - 复制 .env.local 的所有变量
    - 添加新的云服务变量:
      - UPSTASH_REDIS_REST_URL
      - UPSTASH_REDIS_REST_TOKEN
      - INNGEST_EVENT_KEY
      - INNGEST_SIGNING_KEY
      - CLOUDINARY_CLOUD_NAME
      - CLOUDINARY_API_KEY
      - CLOUDINARY_API_SECRET
      - NEXT_PUBLIC_AXIOM_DATASET
      - AXIOM_TOKEN

[ ] 2. 配置 Vercel CLI
    npm install -g vercel
    vercel login
    vercel link  # 关联到 Vercel 项目

[ ] 3. 测试本地部署
    vercel dev  # 启动本地开发服务器
    # 验证所有 API 路由正常

[ ] 4. 推送环境变量到 Vercel
    vercel env pull .env.vercel.local
    vercel env add UPSTASH_REDIS_REST_URL
    vercel env add UPSTASH_REDIS_REST_TOKEN
    # ... 添加所有新变量
```

**输出物**:
- ✅ `.env.vercel` 文件
- ✅ Vercel 项目关联成功
- ✅ 环境变量配置完成

---

#### Day 5-7: 团队培训和代码准备

**任务清单**:

```markdown
[ ] 1. 团队培训会议 (2小时)
    - 讲解云原生架构
    - 演示 Vercel 部署流程
    - 演示 Inngest 任务编排
    - Q&A 环节

[ ] 2. 创建迁移分支
    git checkout -b migration/cloud-native
    git push origin migration/cloud-native

[ ] 3. 代码审查准备
    - 阅读所有迁移文档
    - 标记需要改动的文件
    - 创建迁移任务看板

[ ] 4. 备份当前环境
    - 备份 Docker 镜像
    - 备份数据库 (Supabase 自动备份)
    - 导出当前环境变量
    - 文档化当前部署流程
```

**里程碑**: **M1 - 准备阶段完成** ✅

---

## 🚀 阶段 2: 前端迁移 (Day 8-14)

### 目标
- ✅ Next.js 应用部署到 Vercel
- ✅ Redis 迁移到 Upstash
- ✅ Cron 任务配置

### 详细任务

#### Day 8-9: Vercel 部署

**任务清单**:

```markdown
[ ] 1. 配置 vercel.json
    创建 /vercel.json:
    ```json
    {
      "buildCommand": "npm run build",
      "devCommand": "npm run dev",
      "installCommand": "npm install",
      "framework": "nextjs",
      "regions": ["hkg1", "sin1"],  // 香港 + 新加坡
      "crons": [
        {
          "path": "/api/cron/generate-blog",
          "schedule": "0 10 * * *"
        }
      ]
    }
    ```

[ ] 2. 首次部署到 Vercel
    vercel --prod
    # 等待部署完成

[ ] 3. 验证部署
    - 访问 Vercel 提供的 URL
    - 测试首页加载
    - 测试 API 路由 (/api/health)

[ ] 4. 配置自定义域名
    - 在 Vercel Dashboard 添加域名
    - 更新 DNS 记录 (先用测试子域名)
    - 等待 SSL 证书生成

[ ] 5. 测试 Preview 部署
    - 创建测试 PR
    - 验证 Preview URL 自动生成
    - 测试 Preview 环境功能
```

**输出物**:
- ✅ Vercel 生产环境部署成功
- ✅ 自定义域名配置完成
- ✅ Preview 部署流程验证

---

#### Day 10-11: Redis 迁移

**任务清单**:

```markdown
[ ] 1. 改造 Redis 客户端
    修改 /lib/redis.ts:

    ```typescript
    // 原来的代码
    import Redis from 'ioredis'
    export const redis = new Redis(process.env.REDIS_URL)

    // 新的代码
    import { Redis } from '@upstash/redis'

    export const redis = new Redis({
      url: process.env.UPSTASH_REDIS_REST_URL!,
      token: process.env.UPSTASH_REDIS_REST_TOKEN!,
    })

    // API 完全兼容,无需改其他代码!
    ```

[ ] 2. 安装依赖
    npm install @upstash/redis
    npm uninstall ioredis  # 可选,先保留

[ ] 3. 迁移现有 Redis 数据 (如果有)
    # 使用 redis-cli 导出数据
    redis-cli --rdb dump.rdb

    # 导入到 Upstash (使用脚本)
    # 或者手动清空数据 (如果数据不重要)

[ ] 4. 测试 Redis 功能
    - 测试缓存读写
    - 测试 Session 存储
    - 性能对比测试

[ ] 5. 更新环境变量
    - 删除旧的 REDIS_URL
    - 添加 UPSTASH_REDIS_REST_URL
    - 添加 UPSTASH_REDIS_REST_TOKEN
```

**输出物**:
- ✅ Redis 客户端改造完成
- ✅ 数据迁移完成
- ✅ 功能测试通过

---

#### Day 12-14: Cron 任务迁移

**任务清单**:

```markdown
[ ] 1. 创建 Cron API 路由
    创建 /app/api/cron/generate-blog/route.ts:

    ```typescript
    import { generateAndPublishArticle } from '@/lib/blog/article-generator'
    import { NextRequest } from 'next/server'

    export const runtime = 'nodejs'
    export const maxDuration = 300 // 5分钟超时

    export async function GET(request: NextRequest) {
      try {
        // Vercel Cron 会自动验证，不需要手动检查
        console.log('Cron job started: generate-blog')

        const result = await generateAndPublishArticle()

        return Response.json({
          success: true,
          data: result
        })
      } catch (error) {
        console.error('Cron job failed:', error)
        return Response.json(
          { success: false, error: String(error) },
          { status: 500 }
        )
      }
    }
    ```

[ ] 2. 禁用原来的 node-cron
    修改 /instrumentation.ts:

    ```typescript
    // 注释掉 cron 初始化
    // import { initializeCronJobs } from '@/lib/blog/cron-service'

    export async function register() {
      if (process.env.NEXT_RUNTIME === 'nodejs') {
        // initializeCronJobs() // 禁用
      }
    }
    ```

[ ] 3. 测试 Cron 路由
    # 本地测试
    curl http://localhost:3000/api/cron/generate-blog

    # 验证文章生成成功

[ ] 4. 配置 Vercel Cron
    # 已在 vercel.json 中配置
    # 部署后自动生效

[ ] 5. 验证 Cron 执行
    - 在 Vercel Dashboard 查看 Cron 日志
    - 等待第二天 10:00 自动执行
    - 或手动触发测试
```

**输出物**:
- ✅ Cron API 创建完成
- ✅ node-cron 禁用
- ✅ Vercel Cron 配置生效

**里程碑**: **M2 - 前端迁移完成** ✅

---

## ⚙️ 阶段 3: 后台迁移 (Day 15-21)

### 目标
- ✅ BullMQ 迁移到 Inngest
- ✅ 视频处理迁移到 Cloudinary
- ✅ 日志迁移到 Axiom

### 详细任务

#### Day 15-17: Inngest 任务队列迁移

**任务清单**:

```markdown
[ ] 1. 安装 Inngest SDK
    npm install inngest

[ ] 2. 创建 Inngest 客户端
    创建 /lib/inngest/client.ts:

    ```typescript
    import { Inngest } from 'inngest'

    export const inngest = new Inngest({
      id: 'vidfab',
      name: 'VidFab AI Video Platform',
      eventKey: process.env.INNGEST_EVENT_KEY!,
    })
    ```

[ ] 3. 创建 Inngest Functions
    创建 /lib/inngest/functions/video-processing.ts:

    ```typescript
    import { inngest } from '../client'

    // 视频下载任务
    export const downloadVideo = inngest.createFunction(
      {
        id: 'download-video',
        name: 'Download Video from Wavespeed',
        retries: 3,
        timeout: '10m',
      },
      { event: 'video/download.requested' },
      async ({ event, step }) => {
        const { videoId, url } = event.data

        // Step 1: 下载视频
        const videoBuffer = await step.run('download-from-wavespeed', async () => {
          // 原来的下载逻辑
        })

        // Step 2: 上传到 Supabase
        const uploadResult = await step.run('upload-to-supabase', async () => {
          // 原来的上传逻辑
        })

        // Step 3: 更新数据库
        await step.run('update-database', async () => {
          // 更新状态为 completed
        })

        return { success: true, videoId }
      }
    )

    // 缩略图生成任务
    export const generateThumbnail = inngest.createFunction(
      {
        id: 'generate-thumbnail',
        name: 'Generate Video Thumbnail',
        retries: 3,
        timeout: '2m',
      },
      { event: 'video/thumbnail.requested' },
      async ({ event, step }) => {
        // 缩略图生成逻辑
      }
    )

    // 延迟清理任务
    export const cleanupTempFiles = inngest.createFunction(
      {
        id: 'cleanup-temp-files',
        name: 'Cleanup Temporary Files',
      },
      { event: 'video/cleanup.scheduled' },
      async ({ event, step }) => {
        // 等待 24 小时
        await step.sleep('24h')

        // 清理逻辑
        await step.run('delete-temp-files', async () => {
          // 删除临时文件
        })
      }
    )
    ```

[ ] 4. 创建 Inngest API 路由
    创建 /app/api/inngest/route.ts:

    ```typescript
    import { serve } from 'inngest/next'
    import { inngest } from '@/lib/inngest/client'
    import {
      downloadVideo,
      generateThumbnail,
      cleanupTempFiles,
    } from '@/lib/inngest/functions/video-processing'

    export const { GET, POST, PUT } = serve({
      client: inngest,
      functions: [
        downloadVideo,
        generateThumbnail,
        cleanupTempFiles,
      ],
      signingKey: process.env.INNGEST_SIGNING_KEY!,
    })
    ```

[ ] 5. 改造任务触发逻辑
    修改 /app/api/video/generate/route.ts:

    ```typescript
    // 原来的代码
    await videoQueueManager.addJob('download_video', { videoId, url })

    // 新的代码
    await inngest.send({
      name: 'video/download.requested',
      data: { videoId, url }
    })
    ```

[ ] 6. 测试 Inngest 功能
    - 触发视频生成
    - 在 Inngest Dashboard 查看任务执行
    - 验证重试机制
    - 验证延迟任务

[ ] 7. 迁移所有任务类型
    - download_video → video/download.requested
    - generate_thumbnail → video/thumbnail.requested
    - cleanup_temp → video/cleanup.scheduled
    - update_quota → user/quota.update
```

**输出物**:
- ✅ Inngest Functions 创建完成
- ✅ 所有任务迁移完成
- ✅ 功能测试通过

---

#### Day 18-19: Cloudinary 视频处理迁移

**任务清单**:

```markdown
[ ] 1. 安装 Cloudinary SDK
    npm install cloudinary

[ ] 2. 创建 Cloudinary 配置
    创建 /lib/cloudinary.ts:

    ```typescript
    import { v2 as cloudinary } from 'cloudinary'

    cloudinary.config({
      cloud_name: process.env.CLOUDINARY_CLOUD_NAME!,
      api_key: process.env.CLOUDINARY_API_KEY!,
      api_secret: process.env.CLOUDINARY_API_SECRET!,
    })

    export { cloudinary }
    ```

[ ] 3. 创建缩略图生成服务
    创建 /lib/services/thumbnail-service.ts:

    ```typescript
    import { cloudinary } from '@/lib/cloudinary'

    export class ThumbnailService {
      /**
       * 从视频 URL 生成缩略图
       */
      static async generateFromVideo(videoUrl: string): Promise<string> {
        // 方案 A: 从 Supabase URL 直接生成
        const thumbnailUrl = cloudinary.url(videoUrl, {
          resource_type: 'video',
          type: 'fetch',
          format: 'jpg',
          transformation: [
            { width: 640, height: 360, crop: 'fill' },
            { start_offset: '2' }  // 第 2 秒的帧
          ]
        })

        return thumbnailUrl
      }

      /**
       * 上传视频并生成缩略图 (可选)
       */
      static async uploadAndGenerate(videoBuffer: Buffer) {
        const result = await cloudinary.uploader.upload_stream(
          {
            resource_type: 'video',
            folder: 'vidfab-videos'
          },
          (error, result) => {
            if (error) throw error
            return result
          }
        ).end(videoBuffer)

        return {
          videoUrl: result.secure_url,
          thumbnailUrl: cloudinary.url(result.public_id, {
            resource_type: 'video',
            format: 'jpg',
            transformation: [
              { width: 640, height: 360, crop: 'fill' }
            ]
          })
        }
      }
    }
    ```

[ ] 4. 替换 ffmpeg 逻辑
    修改缩略图生成任务:

    ```typescript
    // 原来的代码 (使用 ffmpeg)
    // import ffmpeg from 'fluent-ffmpeg'
    // ... 复杂的 ffmpeg 逻辑

    // 新的代码 (使用 Cloudinary)
    import { ThumbnailService } from '@/lib/services/thumbnail-service'

    const thumbnailUrl = await ThumbnailService.generateFromVideo(videoUrl)

    // 更新数据库
    await supabase
      .from('user_videos')
      .update({ thumbnail_url: thumbnailUrl })
      .eq('id', videoId)
    ```

[ ] 5. 测试缩略图生成
    - 上传测试视频
    - 验证缩略图生成
    - 对比画质和加载速度
```

**输出物**:
- ✅ Cloudinary 配置完成
- ✅ 缩略图服务创建
- ✅ ffmpeg 替换完成

---

#### Day 20-21: Axiom 日志迁移

**任务清单**:

```markdown
[ ] 1. 安装 Axiom SDK
    npm install next-axiom

[ ] 2. 配置 next.config.mjs
    ```javascript
    import { withAxiom } from 'next-axiom'

    const nextConfig = {
      // 你的配置
    }

    export default withAxiom(nextConfig)
    ```

[ ] 3. 创建日志工具类
    创建 /lib/logger.ts:

    ```typescript
    import { log as axiomLog } from 'next-axiom'

    export const logger = {
      info: (message: string, data?: any) => {
        console.log(message, data)
        axiomLog.info(message, data)
      },

      error: (message: string, error?: any) => {
        console.error(message, error)
        axiomLog.error(message, { error })
      },

      warn: (message: string, data?: any) => {
        console.warn(message, data)
        axiomLog.warn(message, data)
      },
    }
    ```

[ ] 4. 替换所有 console.log
    # 使用 VS Code 全局替换
    console.log → logger.info
    console.error → logger.error
    console.warn → logger.warn

[ ] 5. 测试日志收集
    - 触发各种操作
    - 在 Axiom Dashboard 查看日志
    - 测试日志搜索功能
    - 设置告警规则
```

**输出物**:
- ✅ Axiom 配置完成
- ✅ 日志工具类创建
- ✅ 日志收集验证通过

**里程碑**: **M3 - 后台迁移完成** ✅

---

## 🧪 阶段 4: 测试验证 (Day 22-28)

### 目标
- ✅ 完整功能测试
- ✅ 性能对比测试
- ✅ 压力测试

### 详细任务

#### Day 22-24: 功能测试

**测试清单** (详见 [09-测试验证方案.md](./09-测试验证方案.md)):

```markdown
[ ] 用户认证流程
    - 邮箱注册
    - Google OAuth 登录
    - 验证码登录
    - Session 管理

[ ] 视频生成流程
    - 文本转视频
    - 图像转视频
    - 状态轮询
    - 积分扣除

[ ] 后台任务
    - 视频下载
    - 缩略图生成
    - 延迟清理
    - 配额更新

[ ] 支付流程
    - Stripe Checkout
    - Webhook 处理
    - 订阅管理
    - 积分充值

[ ] 定时任务
    - 博客文章生成
    - Cron 执行验证

[ ] 日志和监控
    - 日志收集
    - 错误追踪
    - 性能监控
```

---

#### Day 25-26: 性能测试

**测试清单**:

```markdown
[ ] 首屏加载速度
    - 使用 Lighthouse
    - 对比 Docker vs Vercel
    - 目标: <1秒

[ ] API 响应时间
    - 测试主要 API 端点
    - 对比 Docker vs Vercel
    - 目标: P95 < 500ms

[ ] 任务队列性能
    - 测试任务执行时间
    - 测试并发处理能力
    - 对比 BullMQ vs Inngest

[ ] 缓存命中率
    - 监控 Redis 命中率
    - 优化缓存策略
    - 目标: >80%
```

---

#### Day 27-28: 压力测试

**测试清单**:

```markdown
[ ] 并发用户测试
    - 使用 k6 或 Artillery
    - 模拟 100 并发用户
    - 监控错误率和响应时间

[ ] 视频生成压力测试
    - 同时生成 50 个视频
    - 验证任务队列处理能力
    - 验证自动扩容

[ ] 数据库压力测试
    - 高并发查询
    - 验证 Supabase 连接池
    - 验证 Redis 缓存

[ ] 长时间稳定性测试
    - 运行 24 小时
    - 监控内存泄漏
    - 监控错误率
```

**里程碑**: **M4 - 测试完成** ✅

---

## 🚢 阶段 5: 正式上线 (Day 29-35)

### 目标
- ✅ 灰度发布
- ✅ 全量上线
- ✅ 监控优化

### 详细任务

#### Day 29-30: 灰度发布 (10% 流量)

**任务清单**:

```markdown
[ ] 1. 配置灰度规则
    - 使用测试子域名 (test.vidfab.ai)
    - 或使用 Vercel Deployment Branches

[ ] 2. 导流 10% 用户
    - 通过 DNS 配置
    - 或通过 CDN 分流规则

[ ] 3. 监控关键指标
    - 错误率
    - 响应时间
    - 任务成功率
    - 用户反馈

[ ] 4. 快速响应问题
    - 建立值班机制
    - 准备回滚方案 (见 10-回滚预案.md)
```

---

#### Day 31-32: 扩大灰度 (50% 流量)

**任务清单**:

```markdown
[ ] 1. 分析 10% 灰度数据
    - 错误率对比
    - 性能对比
    - 成本对比

[ ] 2. 修复发现的问题

[ ] 3. 扩大到 50% 流量

[ ] 4. 继续监控 24 小时
```

---

#### Day 33-34: 全量上线 (100% 流量)

**任务清单**:

```markdown
[ ] 1. 最终检查
    - 所有测试通过
    - 性能达标
    - 成本在预算内

[ ] 2. 更新 DNS
    - 将主域名指向 Vercel
    - 保留旧域名 7 天作为备份

[ ] 3. 关闭 Docker 容器
    - 停止 Docker Compose
    - 保留镜像 2 周作为备份
    - 取消 VPS 订阅 (1 个月后)

[ ] 4. 团队庆祝 🎉
```

---

#### Day 35: 后续优化

**任务清单**:

```markdown
[ ] 1. 性能优化
    - 分析慢查询
    - 优化缓存策略
    - 优化图片加载

[ ] 2. 成本优化
    - 分析实际用量
    - 调整套餐等级
    - 设置用量告警

[ ] 3. 文档更新
    - 更新部署文档
    - 更新运维手册
    - 知识库沉淀

[ ] 4. 团队培训
    - Vercel Dashboard 使用
    - Inngest 监控
    - Axiom 日志查询
```

**里程碑**: **M5 - 正式上线** ✅

---

## 📊 进度跟踪

### 每周 Review

| 周次 | 主要任务 | 完成标准 | 负责人 |
|------|---------|---------|--------|
| Week 1 | 准备阶段 | 所有账户注册完成 | [待指定] |
| Week 2 | 前端迁移 | Vercel 部署成功 | [待指定] |
| Week 3 | 后台迁移 | Inngest 任务正常 | [待指定] |
| Week 4 | 测试验证 | 所有测试通过 | [待指定] |
| Week 5 | 正式上线 | 生产环境稳定 | [待指定] |

### 每日 Stand-up

建议每天 10:00 进行 15 分钟站会:
- 昨天完成了什么
- 今天计划做什么
- 遇到什么问题/风险

---

## 🔗 相关文档

- [01-迁移总览.md](./01-迁移总览.md) - 迁移背景和目标
- [03-成本预算分析.md](./03-成本预算分析.md) - 成本详情
- [05-风险评估和应对.md](./05-风险评估和应对.md) - 风险管理
- [06-迁移检查清单.md](./06-迁移检查清单.md) - 详细检查项
- [09-测试验证方案.md](./09-测试验证方案.md) - 测试用例
- [10-回滚预案.md](./10-回滚预案.md) - 回滚方案
